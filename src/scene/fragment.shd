#version 430

// uniform sampler2D srcTex;
// in vec2 texCoord;
in vec3 out_color;
out vec4 color;


void main() {
     // float c = texture(srcTex, texCoord).x;

     color = vec4(out_color.x, out_color.y, out_color.z, 1.0);
     // color = vec4(1.0, 1.0, 1.0, 1.0);
}
