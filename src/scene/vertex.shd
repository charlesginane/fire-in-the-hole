#version 430

in vec3 position;
uniform vec4 ourColor;
uniform float translate;

out vec4 out_color;
void main() {
	 // texCoord = pos*0.5f + 0.5f;
	 gl_Position = vec4(position.x, position.y + translate, position.z, 1.0);
	 out_color = ourColor;
}
